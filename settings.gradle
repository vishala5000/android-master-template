pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    // Changed from FAIL_ON_PROJECT_REPOS to PREFER_SETTINGS 
    // to avoid conflicts with your existing root build.gradle
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "V MP3 Player"
include ':app'
